name: Test <%= name %>

on:
  push:
    branches:
      - main
    paths:
      - "packages/http_api/**"
      - ".github/workflows/tests_packages_http_api.yaml"
  pull_request:
    branches:
      - main
    paths:
      - "packages/http_api/**"
      - ".github/workflows/tests_packages_http_api.yaml"

permissions:
    contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 24
      - name: Install pnpm
        run: npm install -g pnpm
      - name: Install dependencies
        run: pnpm install
      - name: Run tests
        run: pnpm -F <%= name %> test
